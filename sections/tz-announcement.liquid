{% style %}
  #shopify-section-{{ section.id }} .tz-announcement {
  background-color: {{ section.settings.bg_color }};
  color: {{ section.settings.text_color }};
  border-bottom: 1px solid rgba(0,0,0,0.05);
  position: relative;
  z-index: 1002;
  overflow: hidden;
  line-height: 1.5;
  font-size: {{ section.settings.font_size }}px;
  font-weight: 700;
  }

  #shopify-section-{{ section.id }} .tz-announcement a {
  text-decoration: underline;
  text-underline-offset: 3px;
  color: inherit;
  }
  #shopify-section-{{ section.id }} .tz-announcement a:hover { opacity: 0.8; }

  /* Override global light mode styles - must be more specific */
  body.light #shopify-section-{{ section.id }} .tz-announcement,
  body.light #shopify-section-{{ section.id }} .tz-announcement *,
  body.light #shopify-section-{{ section.id }} .tz-announcement p,
  body.light #shopify-section-{{ section.id }} .tz-announcement span,
  body.light #shopify-section-{{ section.id }} .tz-announcement div,
  body.light #shopify-section-{{ section.id }} .tz-announcement a,
  html.light body #shopify-section-{{ section.id }} .tz-announcement,
  html.light body #shopify-section-{{ section.id }} .tz-announcement *,
  html.light body #shopify-section-{{ section.id }} .tz-announcement p,
  html.light body #shopify-section-{{ section.id }} .tz-announcement span,
  html.light body #shopify-section-{{ section.id }} .tz-announcement div,
  html.light body #shopify-section-{{ section.id }} .tz-announcement a,
  .light #shopify-section-{{ section.id }} .tz-announcement,
  .light #shopify-section-{{ section.id }} .tz-announcement *,
  .light #shopify-section-{{ section.id }} .tz-announcement p,
  .light #shopify-section-{{ section.id }} .tz-announcement span,
  .light #shopify-section-{{ section.id }} .tz-announcement div,
  .light #shopify-section-{{ section.id }} .tz-announcement a,
  html.light #shopify-section-{{ section.id }} .tz-announcement,
  html.light #shopify-section-{{ section.id }} .tz-announcement *,
  html.light #shopify-section-{{ section.id }} .tz-announcement p,
  html.light #shopify-section-{{ section.id }} .tz-announcement span,
  html.light #shopify-section-{{ section.id }} .tz-announcement div,
  html.light #shopify-section-{{ section.id }} .tz-announcement a {
    color: {{ section.settings.text_color }} ;
  }

  /* Dark Mode Support */
  .dark #shopify-section-{{ section.id }} .tz-announcement,
  html.dark #shopify-section-{{ section.id }} .tz-announcement {
    border-bottom-color: #333333 ;
  }

  .dark #shopify-section-{{ section.id }} .tz-announcement *,
  html.dark #shopify-section-{{ section.id }} .tz-announcement * {
    color: inherit;
  }

  /* Light Mode Support */
  .light #shopify-section-{{ section.id }} .tz-announcement,
  html.light #shopify-section-{{ section.id }} .tz-announcement {
    border-bottom-color: rgba(0,0,0,0.05) ;
  }

  /* --- STATIC MODE --- */
  #shopify-section-{{ section.id }} .tz-announcement-inner {
  padding: 10px 40px;
  display: flex;
  justify-content: center;
  align-items: center;
  min-height: 40px;
  flex-wrap: wrap;
  gap: 30px;
  }

  /* --- MARQUEE MODE --- */
  #shopify-section-{{ section.id }} .marquee-window {
  display: block;
  overflow: hidden;
  user-select: none;
  padding: 10px 0;
  direction: ltr; /* Force LTR for animation physics */
  }

  #shopify-section-{{ section.id }} .marquee-mover {
  display: flex;
  width: fit-content;
  flex-shrink: 0;
  /* Move from 0 to -50% (halfway) to create infinite loop effect */
  animation: marquee-scroll-{{ section.id }} {{ section.settings.speed }}s linear infinite;
  }

  #shopify-section-{{ section.id }} .marquee-content-set {
  display: flex;
  align-items: center;
  flex-shrink: 0;
  }

  #shopify-section-{{ section.id }} .marquee-item {
  display: inline-flex;
  align-items: center;
  padding: 0 40px;
  white-space: nowrap;
  direction: rtl; /* Restore RTL for Hebrew text */
  }

  #shopify-section-{{ section.id }} .tz-announcement:hover .marquee-mover {
  animation-play-state: paused;
  }

  @keyframes marquee-scroll-{{ section.id }} {
  0% { transform: translateX(0); }
  100% { transform: translateX(-50%); }
  }
{% endstyle %}

<!-- CAPTURE CONTENT -->
{% capture announcement_text %}
{%- for i in (1..10) -%}
{%- for block in section.blocks -%}
  {%- liquid
    assign block_bg = block.settings.bg_color | default: 'transparent'
    assign block_text = block.settings.text_color | default: 'inherit'
  -%}
<div class="marquee-item uppercase" 
     style="{% if block.settings.bg_color != blank %}background-color: {{ block_bg }};{% endif %} {% if block.settings.text_color != blank %}color: {{ block_text }};{% endif %}"
     {% if i==1 and forloop.first %}{{ block.shopify_attributes }}{% endif %}>
    {%- if block.settings.link != blank -%}
    <a href="{{ block.settings.link }}" style="color: inherit;">{{ block.settings.text }}</a>
    {%- else -%}
    <span>{{ block.settings.text }}</span>
    {%- endif -%}
</div>
{%- endfor -%}
{%- endfor -%}
{% endcapture %}

<div class="tz-announcement" role="region" aria-label="Announcement">
  {% if section.settings.enable_marquee %}
    <!-- MARQUEE -->
    <div class="marquee-window">
      <div class="marquee-mover">
        <div class="marquee-content-set">
          {{ announcement_text }}
        </div>
        <div class="marquee-content-set" aria-hidden="true">
          {{ announcement_text }}
        </div>
      </div>
    </div>
  {% else %}
    <!-- STATIC -->
    <div class="tz-announcement-inner">
      {%- for block in section.blocks -%}
        {%- liquid
          assign block_bg = block.settings.bg_color | default: 'transparent'
          assign block_text = block.settings.text_color | default: 'inherit'
        -%}
        <div class="uppercase" 
             style="padding: 5px 15px; {% if block.settings.bg_color != blank %}background-color: {{ block_bg }};{% endif %} {% if block.settings.text_color != blank %}color: {{ block_text }};{% endif %}"
             {{ block.shopify_attributes }}>
          {%- if block.settings.link != blank -%}
            <a href="{{ block.settings.link }}" style="color: inherit;">{{ block.settings.text }}</a>
          {%- else -%}
            <span>{{ block.settings.text }}</span>
          {%- endif -%}
        </div>
      {%- endfor -%}
    </div>
  {% endif %}
</div>

{% schema %}
{
  "name": "Marquee Announcement",
  "class": "section-marquee-announcement",
  "enabled_on": {
    "groups": ["header"]
  },
  "settings": [
    {
      "type": "checkbox",
      "id": "enable_marquee",
      "label": "Enable Marquee",
      "default": true
    },
    {
      "type": "select",
      "id": "speed",
      "label": "Animation Speed",
      "default": "30",
      "info": "Higher values = slower animation",
      "options": [
        {
          "value": "30",
          "label": "Slow (30s) - Default"
        },
        {
          "value": "5",
          "label": "Very Fast (5s)"
        },
        {
          "value": "10",
          "label": "Fast (10s)"
        },
        {
          "value": "15",
          "label": "Medium Fast (15s)"
        },
        {
          "value": "20",
          "label": "Medium (20s)"
        },
        {
          "value": "25",
          "label": "Medium Slow (25s)"
        },
        {
          "value": "40",
          "label": "Very Slow (40s)"
        },
        {
          "value": "50",
          "label": "Extra Slow (50s)"
        },
        {
          "value": "60",
          "label": "Ultra Slow (60s)"
        },
        {
          "value": "80",
          "label": "Very Ultra Slow (80s)"
        },
        {
          "value": "100",
          "label": "Extremely Slow (100s)"
        },
        {
          "value": "120",
          "label": "Maximum Slow (120s)"
        }
      ]
    },
    {
      "type": "color",
      "id": "bg_color",
      "label": "Background Color",
      "default": "#d32f2f"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text Color",
      "default": "#ffffff"
    },
    {
      "type": "range",
      "id": "font_size",
      "min": 10,
      "max": 20,
      "step": 1,
      "unit": "px",
      "label": "Font Size",
      "default": 13
    }
  ],
  "blocks": [
    {
      "type": "announcement",
      "name": "Message",
      "settings": [
        {
          "type": "text",
          "id": "text",
          "label": "Text",
          "default": "Welcome to our store"
        },
        {
          "type": "url",
          "id": "link",
          "label": "Link"
        },
        {
          "type": "color",
          "id": "bg_color",
          "label": "Background Color (Overrides section)"
        },
        {
          "type": "color",
          "id": "text_color",
          "label": "Text Color (Overrides section)"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Marquee Announcement",
      "blocks": [{ "type": "announcement" }, { "type": "announcement" }]
    }
  ]
}
{% endschema %}

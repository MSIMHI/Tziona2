// TZ Contact Form JS
document.addEventListener('DOMContentLoaded', () => {
  const contactForm = document.getElementById('ContactForm');
  if (contactForm) {
    // Optional: Add client-side validation or enhancements here
    console.log('TZ Contact Form initialized');
  }
});
